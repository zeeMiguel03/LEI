<!DOCTYPE html>
<html lang="pt">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Formulário Bootstrap</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
        <link rel='stylesheet' href='/stylesheets/result.css' />
    </head>

    <body>
        <div class="container mt-4">
            <h1>Review Results</h1>
    
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                <% if (reviews.length > 0) { %>
                    <% reviews.forEach((review) => { %>
                        <div class="col">
                            <div class="card h-100">
                                <div class="card-body">
                                    <h5 class="card-title">Book Name: <%= review.book %></h5>
                                    <h6 class="card-subtitle mb-2 text-muted">By <%= review.name %></h6>
                                    <p class="card-text">Description: <%= review.description %></p>
                                    <p class="card-text">Status: <%= review.inlineRadioOptions === 'read' ? 'Read' : 'Not Read' %></p>
                                    <p class="card-text">Email: <%= review.email %></p>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p>No reviews found for this book and email.</p>
                <% } %>
            </div>
    
            <a href="/" class="btn btn-primary mt-4">Go back</a>
        </div>
    
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>



